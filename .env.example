# KryllBot VPS Configuration
# Copy to .env and fill in values: cp .env.example .env

# Docker image (default: pull from GHCR)
KRYLLBOT_IMAGE=ghcr.io/phlongere/openclaw-vps:latest

# Gateway token (auto-generated by setup-vps.sh, or generate with: openssl rand -hex 32)
KRYLLBOT_GATEWAY_TOKEN=change-me

# Gateway binding (lan = all interfaces inside container, accessed via loopback on host)
KRYLLBOT_GATEWAY_BIND=lan
KRYLLBOT_GATEWAY_PORT=18789
KRYLLBOT_BRIDGE_PORT=18790

# Persistent directories on host
KRYLLBOT_CONFIG_DIR=/home/kryllbot/.openclaw
KRYLLBOT_WORKSPACE_DIR=/home/kryllbot/.openclaw/workspace

# === User-provided config ===

# Agent name (display name for the bot)
KRYLLBOT_AGENT_NAME=Assistant

# AI Provider API key (OpenAI, Anthropic, etc.)
OPENAI_API_KEY=change-me

# Telegram bot config
TELEGRAM_BOT_TOKEN=change-me
TELEGRAM_ALLOWED_USER=change-me
